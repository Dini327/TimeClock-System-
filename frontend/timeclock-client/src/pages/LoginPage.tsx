import { Button } from "@mui/material";

function LoginPage() {
  return <div>
    Login Page — coming soon
  <Button variant="contained" color="primary" onClick={() => alert("Login functionality coming soon!")}></Button>
  </div>;
}

export default LoginPage;
